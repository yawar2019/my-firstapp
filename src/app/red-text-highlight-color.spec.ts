import { RedTextHighlightColor } from './red-text-highlight-color';

describe('RedTextHighlightColor', () => {
  it('should create an instance', () => {
    const directive = new RedTextHighlightColor();
    expect(directive).toBeTruthy();
  });
});
